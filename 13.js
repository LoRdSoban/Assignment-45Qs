"use strict";
var fav_transport = ["Car", "Motorcycle", "Bicycle", "Airplane", "Train"];
for (let i = 0; i < fav_transport.length; i++) {
    console.log(`I would like to own a ${fav_transport[i]}`);
}
